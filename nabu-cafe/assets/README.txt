Place this business's photos and any extracted CSS here (e.g. hero.jpg, logo.png, styles.css).
The current index.html is self-contained (CSS is embedded, fonts + map load online), so this folder starts empty.
